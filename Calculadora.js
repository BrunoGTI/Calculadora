var mostrar = visor;

function botao( num ){
	var salva = document.Calc.visor.value;
	document.Calc.visor.value = salva + num;
}
function rasultado (){
	var resul = document.Calc.visor;
	if{
		document.Calc.visor = eval(resul);
	}
}